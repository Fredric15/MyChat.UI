@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

@* <div  class="text-center">
    <h1 class="display-4">Welcome to MyChat!</h1>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show auto-expire" role="alert">
            <i class="bi bi-check-circle-fill me-2"></i> @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show auto-expire mt-3" role="alert">
            <i class="bi bi-exclamation-triangle-fill me-2"></i> @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-5 col-lg-4">
                <div class="card shadow border-0 mt-5">
                    <div class="card-body p-5">
                        <h2 class="text-center fw-bold mb-4">Logga in</h2>

                        <form method="post">
                            <div class="mb-3">
                                <label class="form-label small fw-bold">ANVÄNDARNAMN</label>
                                <input asp-for="UserName" class="form-control form-control-lg"/>
                            </div>

                            <div class="mb-4">
                                <label class="form-label small fw-bold">LÖSENORD</label>
                                <input asp-for="Password" class="form-control form-control-lg" type="password" />
                            </div>

                            <button type="submit" class="btn btn-primary btn-lg w-100 shadow-sm mb-3">Logga in</button>
                        </form>

                        <hr />

                        <div class="text-center mt-4">
                            <p class="text-muted small mb-2">Har du inget konto?</p>
                            <a asp-page="./Register" class="btn btn-outline-secondary w-100">Skapa nytt konto</a>
                        </div>
                    </div>
                </div>
                <div class="text-center mt-3">
                    <a asp-page="/Index" class="text-decoration-none small text-muted">← Tillbaka till hem</a>
                </div>
            </div>
        </div>
    </div>
</div> *@

<div class="container d-flex flex-column justify-content-center align-items-center" style="min-height: 85vh;">

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show auto-expire shadow-sm mb-4" role="alert" style="width: 100%; max-width: 450px;">
            <i class="bi bi-check-circle-fill me-2"></i> @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="text-center mb-5">
        <h1 class="display-3 fw-bold text-dark mb-0" style="letter-spacing: -2px;">MyChat<span class="text-primary">.</span></h1>
        <p class="text-muted lead">Anslut, snacka och ha kul.</p>
    </div>

    <div class="card login-card p-4 p-md-5 bg-white" style="width: 100%; max-width: 450px;">
        <h3 class="text-center fw-bold mb-4">Logga in</h3>

        <form method="post">
            <div class="form-group mb-3">
                <label class="form-label small fw-bold text-muted">ANVÄNDARNAMN</label>
                <div class="input-group">
                    <span class="input-group-text bg-light border-0"><i class="bi bi-person text-primary"></i></span>
                    <input asp-for="UserName" type="text" class="form-control bg-light border-0 py-2" placeholder="Skriv ditt namn..." />
                </div>
                <span asp-validation-for="UserName" class="text-danger small m-3"></span>
            </div>

            <div class="form-group mb-4">
                <label class="form-label small fw-bold text-muted">LÖSENORD</label>
                <div class="input-group">
                    <span class="input-group-text bg-light border-0"><i class="bi bi-lock text-primary"></i></span>
                    <input asp-for="Password" type="password" class="form-control bg-light border-0 py-2" placeholder="Ditt lösenord..." />
                </div>
                <span asp-validation-for="Password" class="text-danger small m-3"></span>
                <div asp-validation-summary="ModelOnly" class="text-danger small m-3"></div>
            </div>

            <div class="d-grid gap-2">
                <button type="submit" class="btn btn-primary btn-lg shadow-sm py-3 fw-bold">
                    Logga in <i class="bi bi-arrow-right ms-2"></i>
                </button>
            </div>
        </form>

        <div class="text-center mt-4">
            <p class="small text-muted mb-1">Har du inget konto?</p>
            <a asp-page="/Register" class="text-decoration-none fw-bold">Skapa ett nytt konto nu</a>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Vänta tills sidan har laddats helt
        document.addEventListener("DOMContentLoaded", function() {
            // Hitta alla element med klassen 'auto-expire'
            const alerts = document.querySelectorAll('.auto-expire');

            alerts.forEach(function(alert) {
                // Skapa en timer (5000 millisekunder = 5 sekunder)
                setTimeout(function() {
                    // Använd Bootstraps inbyggda metod för att stänga meddelandet
                    const bsAlert = new bootstrap.Alert(alert);
                    bsAlert.close();
                }, 3000);
            });
        });
    </script>
}
